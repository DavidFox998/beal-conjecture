# Kolyvagin / Fintype Subsingleton display (v28)

See [`Kolyvagin_Fintype_Subsingleton_v28.md`](Kolyvagin_Fintype_Subsingleton_v28.md).

Inhabits the typeclass upgrade `Nonempty (Fintype α) → Fintype α`,
Subsingleton card `1` on `Unit`, `|Sel₂|=1`, `3·7=21`,
`L/Ω=1/3` and `1/7`, and `¬IsRankZero` on Cremona `26a1`/`26b1`.

`IsRankZero := Subsingleton` is false on those MW groups
(torsion 3 and 7). Parent `MW_rank_zero_fintype` stays
`def Prop`. Foundations branch
`kolyvagin-fintype-subsingleton-v28` at `b01a399`.
