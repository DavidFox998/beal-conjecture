\section{Mazur --- no rational $13$-isogeny}\label{sec:mazur}

The file
\texttt{Mazur\_X0\_13\_No\_Isogeny\_final.lean}
(\texttt{2f4e16e}; incremental
\href{https://github.com/DavidFox998/beal-level-26-foundations/actions/runs/35488699511}{35488699511},
$252$s) displays the Mazur constants for $X_0(13)$~\cite{mazur1978}.

\begin{displaythm}[Mazur numerals]
\label{disp:mazur}
\[
\begin{aligned}
  \lvert\SL_2(\F_{13})\rvert&=13\cdot 12\cdot 14=2184
    &&(\texttt{SL2\_F13\_card}),\\
  48&<2184
    &&(\texttt{forty\_eight\_lt\_2184}),\\
  13&=2^2+3^2
    &&(\texttt{thirteen\_eq\_two\_squares}),\\
  288/48&=6
    &&(\texttt{two\_eighty\_eight\_div\_forty\_eight}),\\
  (\{0,1\}:\mathrm{Finset}\,\mathbb{N}).\mathrm{card}&=2
    &&(\texttt{displayed\_cusps\_card}).
\end{aligned}
\]
The first four identities are axiom-free \texttt{decide} theorems.
The cusp-card theorem uses $[\mathtt{propext},\mathtt{Quot.sound}]$.
\end{displaythm}

The parent names \texttt{X0\_13\_Q\_infinite},
\texttt{frey\_no\_rational\_13\_isogeny},
\texttt{Serre\_non\_Borel\_mod13}, and
\texttt{mazur\_no\_Frey\_13\_isogeny} are theorems in this
namespace. Each re-exports the inhabited conjunction of
Display~\ref{disp:mazur} with the genus Nat $0$. On the parent
file \texttt{Mazur\_X0\_13\_No\_Isogeny.lean} those four names stay
\texttt{def Prop}.

\begin{honesty}[Genus $0$ versus two cusps]
$X_0(13)$ has genus $0$, so $X_0(13)(\Q)$ is infinite over $\Q$.
The pasted equality $\{2\text{ cusps}\}=X_0(13)(\Q)$ is
literature-false and is \emph{not} a theorem. The honest Lean
display is finite card~$2$ versus the genus-$0$ note
\texttt{X0\_13\_genus\_nat $\neq$ X0\_13\_cusp\_count}. The theorem
named \texttt{X0\_13\_Q\_infinite} packages that conjunction; it is
not a Mathlib proof that the rational points are infinite.
\end{honesty}

Geometrically, a genus-$0$ curve has trivial Jacobian, so $J_0(13)$
is a point (rank~$0$, finite). That remark is not claimed as a Lean
Mordell--Weil theorem in the display file. Mazur's theorem that the
only rational $13$-isogenies of elliptic curves over $\Q$ arise from
the cusps remains \texttt{def Prop} on the parent.

Axioms: numerals $[\mathtt{propext}]$ or axiom-free;
package $[\mathtt{propext},\mathtt{Quot.sound}]$;
LLL no-go persistence
$[\mathtt{propext},\mathtt{Classical.choice},\mathtt{Quot.sound}]$.
No \texttt{native\_decide}, no \texttt{sorryAx}, no new axiom.

\subsection{Point-level $X_0(13)$, Frey $j$, and the Borel boundary}

The companion module \path{MazurCurve.X0_13FreyJBorel} at commit
\texttt{0a52ba8af887724bcd36df6433a599452e6eee74} records the argument in
forward construction order. Its type \path{FrickeCusp} has exactly two
constructors, but this exact cusp table is not identified with all of
$X_0(13)(\Q)$. A value of \path{X0_13_Moduli_Data} instead supplies a
concrete point-level object, a nonzero Fricke parameter $t$, and the equality
\[
 j(t)=
 \frac{(t^2+5t+13)(t^4+7t^3+20t^2+19t+1)^3}{t}.
\]
Likewise, \path{FreyJInvariantData} attaches to that construction the exact
identity
\[
 j(E_{A,B})=\frac{c_4(E_{A,B})^3}{\Delta(E_{A,B})}.
\]

The proposition \path{BorelReducibilityForward} is the required
reducibility/Fricke-image equivalence, not a consequence of
$48<2184$. The theorem
\path{frey_no_rational_13_isogeny_forward} takes this equivalence and the
separate Frey-specific exclusion of rational Fricke parameters under
$29\mid B+3$. Only then does it project the absence of a rational cyclic
$13$-isogeny. Thus the proved genus, cusp, discriminant, valuation,
image-cardinality, and $32/928$ numerals never manufacture the missing
geometry or global argument.
