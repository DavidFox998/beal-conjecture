Beacon — PR #5 is v8.0.0 Taylor-Wiles patching, not v7.3.1 docs — fix narrative and continue

Context from screenshot:
- v7.3.1 already tagged remotely at 5f2aa7e, CI #244/#245 green on main — no new v7.3.1 tag needed
- PR #5 https://github.com/DavidFox998/beal-conjecture/pull/5 is v8.0.0: replace hRank with explicit Taylor-Wiles patching data — CI still pending
- Task #444 marked merged, v8 work represented by PR #5 — you correctly held merge to avoid publishing v8 patching under v7.3.1 docs narrative

Action:

1. Rename PR #5 correctly:
   gh pr edit 5 --title "v8.0.0: replace hRank with explicit Taylor-Wiles patching data" --body "v8.0.0 Task #443: Close hRank LocalizedRankOne via explicit TW patching data M_∞ over S_∞. Replaces explicit Prop hRank with explicit specialization boundary from real deformation/Hecke geometry. Builds on v7.3.1 5f2aa7e [propext, Quot.sound] focused bridge vs 2 intentional axioms mazur+wiles. Skeleton Ihara (α_p,β_p injectivity) + Shimura (Hecke stable + genuineFormGeneration) remain as explicit clean data fields in EutheosGeometry for #441/#442 future. Audit must remain: 0 opaque, repo-wide ^axiom = 2 intentional only, focused [propext, Quot.sound]. CI must pass like #245."

2. Fix branch to match v8.0.0 scope:
   git checkout <branch for PR #5>
   git pull
   Ensure Beal/Patching/ exists with explicit data boundary, not full proof yet:
      - Beal/Patching/TaylorWilesPrimes.lean — Q_n from Brothers1419 using ‖p·α₀‖<1/p
      - Beal/Patching/PatchedModule.lean — M_n, M_∞ explicit data
      - Beal/Patching/RankOne.lean — LocalizedRankOne_from_Patching uses wiles_lifting_axiom + mazur_axiom + explicit data
   Ensure hRank in MainTheorem is now explicit boundary data, not Prop

3. Push and wait CI:
   git push
   gh pr checks 5 --watch --interval 60

4. When CI green (~1h10m like #243-#245), merge as v8.0.0 start:
   gh pr merge 5 --merge --delete-branch=false

5. Then after merge:
   git checkout main
   git pull
   lake build Beal
   grep -r "^axiom" Beal/ --include="*.lean" -- 2 intentional
   grep -r "^opaque" -- 0

This starts v8.0.0 correctly — no v7.3.1 retag, PR #5 is the v8 patching PR.