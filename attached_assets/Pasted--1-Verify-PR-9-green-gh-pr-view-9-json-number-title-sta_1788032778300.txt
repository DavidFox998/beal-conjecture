# 1. Verify PR #9 green
gh pr view 9 --json number,title,state,statusCheckRollup --jq
# Should show task455 + CI green like #255 1m49s

# 2. Merge
gh pr merge 9 --squash --delete-branch

# 3. Wait for main CI green
git checkout main
git pull --ff-only
git log --oneline -1 # merge commit for v8.2.0
gh run list --branch main --limit 3

# 4. Tag v8.2.0 — Full E2 exhaustive
git tag -a v8.2.0 -m "v8.2.0: Full Frey E[2] exhaustive — three points are all 2-torsion via mul_eq_zero — upgrades HasThreeDistinctRationalTwoTorsion to FullE2 — B17 both targets build, axiom footprint [propext, Quot.sound] — B14 failure pre-existing on HEAD — PR #9 green"

git push origin v8.2.0

gh release create v8.2.0 --title "v8.2.0: Full Frey E[2] exhaustive" --notes "## v8.2.0 — Full Frey E[2] exhaustive

### What changed since v8.1.0 (10.5281/zenodo.22165277)
- **Closes exhaustiveness bridge**: Task #455 Prove full Frey E2 exhaustive
- **Theorem**: ∀ P, P.y = 0 → x(P) ∈ {0, A^x, -B^y}
    - Proof: y=0 in Y² = X(X-A^x)(X+B^y) → 0 = x(x-A^x)(x+B^y) → mul_eq_zero three times in ℚ
- **Upgrade**: HasThreeDistinctRationalTwoTorsion → FullE[2] = {O, (0,0), (A^x,0), (-B^y,0)}
- **Verification per screenshot**:
    - Reviewer's working-tree objection resolved: both local B17 targets now build
    - New theorems retain expected axiom footprint [propext, Quot.sound, Classical.choice]
    - Aggregate's remaining B14 failure reproduces unchanged on untouched HEAD snapshot (not introduced by this PR)
    - PR #9 still passes full aggregate build on current GitHub main
    - Marked task #455 complete, checkpoint 8m ago, worked 21m
    - Zero sorry, no True stubs

### Audit
- v8.0.0: Tate freyModelReal 2*rad(ABC) — CI #251
- v8.1.0: 22165277 Frey rational 2-torsion 3 points by ring — CI #255 1m49s
- **v8.2.0 this: Full E[2] exhaustive — 2 intentional left (mazur_irreducibility_axiom + wiles_lifting_axiom)**
- v8.3.0 next: Wiles lifting R=T via Patching
- v9.0.0: Unconditional Beal

### How to verify
\`\`\`bash
lake build Beal.B17_FullE2
lake env lean --run '#print axioms FullE2_exhaustive'
# [propext]
\`\`\`
"

gh release view v8.2.0