# `docs/release/`

**v26.** Historical notes (`v7.2.0`, `v8.3.0`, `v11.0.0`) stay
as written. The prepared mint string is
`v26.0.0-level-26-foundations-certified-mw-rank-zero` and is
**not minted** in this documentation pass.

This folder preserves the release history of the Beal chamber in *Opera
Numerorum*. Each release note records what changed in the formal instrument,
which checks are reproducible, and which mathematical boundaries remain open.

The older notes are historical snapshots, not promises that later work has
retroactively strengthened them. Read them alongside the current root
README, the Lean module documentation, and the committed certificate data.
The release sequence is an audit trail: a new finite check can sharpen the
map without becoming an unconditional Beal proof.